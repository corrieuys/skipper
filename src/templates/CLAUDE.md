# src/templates

Task template helpers — template config → phase overrides + hooks.

| file | use |
|---|---|
| `helpers.ts` | `getTaskTemplateId()`, `getTaskHooks()`, `getTemplateSkipperPrompt()`, `getTemplatePhasePrompt()` |

Templates persisted in `task_templates` table. Hook payload + phase-override prompts on the template.
