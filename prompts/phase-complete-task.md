TASK COMPLETION (root Skipper only — delegated agents do NOT close tasks):
When you have completed this task, first call `mcp__skipper-daemon__create_note` (or the bare `create_note` form if your tool list shows it) with a simple breakdown of the relevant details, outcomes, risks, or follow-up context that might matter, then call `mcp__skipper-daemon__complete_task({ summary })`.
