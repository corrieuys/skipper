SHARED MEMORY: this task is a run of a recurring task, and the memory is shared across ALL of its runs (earlier runs included, up to the series' retention window). Each hit's `run` field tells you which run it came from (`this_run: true` marks your own). Results include at most a few entries per run, so widen `limit` or filter with `run_id` / `since` when you need depth on one run or period. Use `scope: "run"` to search only this run. What an earlier run did or decided may no longer apply today: prefer the newest entries, and treat anything from an earlier run as a lead to verify, never as a fact about the current run.
